{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests": "newman run api_tests/BillingOrderAPI.postman_collection.json -e api_tests/BillingOrder.postman_environment.json -r cli,html --reporter-html-template api_tests/templates/htmlreqres.hbs --reporter-html-export report.html --reporter-junit-export newman.xml"
  },
  "author": "Ajit Sharma",
  "dependencies": {
    "newman": "^5.0.0"
  }
}
